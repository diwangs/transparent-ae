{
  "name": "react-src",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@khanacademy/flow-to-ts": "^0.5.2"
  },
  "scripts": {
    "convert": "scripts/convert_src.sh",
    "patch": "scripts/patch_src.sh",
    "build:src": "scripts/build_db_src.sh",
    "build:ts": "scripts/build_db_ts.sh",
    "build": "yarn convert && yarn patch && yarn build:src && yarn build:ts",
    "targetinstall": "cd react && COREPACK_INTEGRITY_KEYS='' COREPACK_ENABLE_DOWNLOAD_PROMPT=0 yarn install --frozen-lockfile && cd .."
  }
}
