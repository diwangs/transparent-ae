{
    "name": "angular-src",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "build": "scripts/build_db.sh",
      "targetinstall": "cd angular && yarn install --frozen-lockfile && cd .."
    }
  }
  